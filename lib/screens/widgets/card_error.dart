import 'package:flutter/material.dart';

class CoinDetailedCardError extends StatelessWidget {
  const CoinDetailedCardError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
