import 'package:flutter/material.dart';

class CoinDetailedCardLoading extends StatelessWidget {
  const CoinDetailedCardLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const CircularProgressIndicator(color: Colors.blue);
}
