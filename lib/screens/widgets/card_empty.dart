import 'package:flutter/material.dart';

class CoinDetailedCardEmpty extends StatelessWidget {
  const CoinDetailedCardEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) =>
      Center(child: const Text('empty'));
}
